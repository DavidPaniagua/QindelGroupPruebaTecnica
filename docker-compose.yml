services:
  pricing-api:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    container_name: pricing-api
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default